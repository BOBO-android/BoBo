<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (AppCompat) -->
    <style name="Base.Theme.BoBo" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Default Theme (AppCompat) -->
    <style name="Theme.BoBo" parent="Base.Theme.BoBo" />

    <!-- New Material Theme -->
    <style name="Theme.BoBo.Material" parent="Theme.MaterialComponents.Light.NoActionBar">
    </style>
</resources>
